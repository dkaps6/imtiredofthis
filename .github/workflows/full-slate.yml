name: Full Slate

on:
  workflow_dispatch:
    inputs:
      season:
        description: "NFL season"
        required: false
        default: "2026"
      prior_season:
        description: "Prior season used for historical priors/backfills"
        required: false
        default: "2025"
      date:
        description: "Slate date (YYYY-MM-DD) or blank for nearest upcoming NFL week"
        required: false
        default: ""
      fetch_live_odds:
        description: "Fetch live player props/game odds (USES ODDS API CREDITS)"
        required: false
        type: boolean
        default: false

concurrency:
  group: full-slate-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  PYTHONDONTWRITEBYTECODE: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  APISPORTS_KEY: ${{ secrets.APISPORTS_KEY }}
  ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
  ESPN_COOKIE: ${{ secrets.ESPN_COOKIE }}
  MSF_KEY: ${{ secrets.MSF_KEY }}
  MSF_PASSWORD: ${{ secrets.MSF_PASSWORD }}
  NFLGSIS_USERNAME: ${{ secrets.NFLGSIS_USERNAME }}
  NFLGSIS_PASSWORD: ${{ secrets.NFLGSIS_PASSWORD }}
  SEASON: ${{ github.event.inputs.season }}
  PRIOR_SEASON: ${{ github.event.inputs.prior_season }}
  SLATE_DATE: ${{ github.event.inputs.date }}
  FETCH_LIVE_ODDS: ${{ github.event.inputs.fetch_live_odds }}

jobs:
  full_slate:
    name: Full slate (single job)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nflreadpy pyyaml

      - name: Validate season context
        run: |
          export PYTHONPATH="${PWD}"
          python - <<'EOF'
          import os
          from scripts.config import SEASON, PRIOR_SEASON, SLATE_DATE
          assert SEASON == int(os.environ["SEASON"])
          assert PRIOR_SEASON == int(os.environ["PRIOR_SEASON"])
          assert PRIOR_SEASON < SEASON
          assert SLATE_DATE == os.environ.get("SLATE_DATE", "").strip()
          print(f"current={SEASON} prior={PRIOR_SEASON} slate={SLATE_DATE or '<latest>'}")
          EOF

      - name: Live odds mode status
        run: |
          if [ "${{ github.event.inputs.fetch_live_odds }}" = "true" ]; then
            echo "LIVE ODDS ENABLED: this run may consume Odds API credits."
          else
            echo "LIVE ODDS PAUSED: player-prop/game-odds API calls will NOT run and will consume zero Odds API credits."
          fi

      - name: Build Ourlads roles
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/providers/ourlads_depth.py --season "${SEASON}"
          mkdir -p outputs
          cp data/roles_ourlads.csv outputs/roles_ourlads.csv
          test -s data/roles_ourlads.csv

      - name: Build authoritative team-week map
        env:
          TEAM_WEEK_MAP_CSV: data/team_week_map.csv
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/utils/build_team_week_map_v2.py --season "${SEASON}" --out data/team_week_map.csv
          test -s data/team_week_map.csv
          python -c "from scripts.runtime_context import log_runtime_context,resolve_week; log_runtime_context(); print('week=',resolve_week())"

      - name: Fetch player props and game odds
        if: ${{ github.event.inputs.fetch_live_odds == 'true' }}
        env:
          TEAM_WEEK_MAP_CSV: data/team_week_map.csv
          ROLES_CSV: data/roles_ourlads.csv
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/fetch_props_oddsapi.py --season "${SEASON}" --date "${SLATE_DATE}"
          test -s outputs/props_raw.csv
          test -s outputs/odds_game.csv
          test -s data/opponent_map_from_props.csv || { echo "ERROR: fetch_props_oddsapi did not materialize opponent_map_from_props.csv"; exit 1; }

      - name: Validate live opponent map
        if: ${{ github.event.inputs.fetch_live_odds == 'true' }}
        run: |
          export PYTHONPATH="${PWD}"
          python - <<'EOF'
          import pandas as pd
          p = "data/opponent_map_from_props.csv"
          df = pd.read_csv(p)
          if df.empty: raise SystemExit(f"FATAL: {p} is empty")
          required = {"team", "opponent"}
          missing = required - set(df.columns)
          if missing: raise SystemExit(f"FATAL: {p} missing columns {sorted(missing)}")
          unresolved = int(df["team"].isna().sum() + df["opponent"].isna().sum())
          if unresolved: raise SystemExit(f"FATAL: {p} contains {unresolved} unresolved team/opponent cells")
          print(f"opponent map OK: rows={len(df)}")
          EOF

      - name: Build TeamForm with explicit preseason priors
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/providers/sharpfootball_pull.py --season "${SEASON}" --dump-html
          python scripts/team_form_prior_bridge.py --season "${SEASON}" --prior-season "${PRIOR_SEASON}"
          python scripts/run_team_form_context.py --season "${SEASON}"
          test -s data/team_form.csv
          python - <<'EOF'
          import pandas as pd
          df = pd.read_csv("data/team_form.csv")
          if df.empty: raise SystemExit("FATAL: team_form.csv is empty")
          if df["season"].astype(int).nunique() != 1 or int(df["season"].astype(int).iloc[0]) != int(__import__('os').environ['SEASON']):
              raise SystemExit("FATAL: TeamForm season context is not the active season")
          print("TeamForm provenance:", df[[c for c in ["team_form_active_season","team_form_prior_season","pbp_feature_season","pbp_prior_used","pbp_feature_source"] if c in df.columns]].drop_duplicates().to_dict("records"))
          EOF

      - name: Build weather and injuries
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/build/build_weather_week_v2.py --season "${SEASON}" --date "${SLATE_DATE}"
          python scripts/build/build_injuries_weekly.py data/injuries.csv --season "${SEASON}"
          test -s data/weather_week.csv
          test -s data/injuries.csv
          python - <<'EOF'
          import pandas as pd
          df = pd.read_csv("data/injuries.csv")
          required = {"player","team","season","week","status","practice_status","body_part","designation","report_date","source","report_available"}
          missing = required - set(df.columns)
          if missing: raise SystemExit(f"FATAL: injuries.csv missing columns {sorted(missing)}")
          print(f"injuries artifact OK: rows={len(df)} (zero is valid before official reports exist)")
          EOF

      - name: Build Coverage v2 matchup intelligence
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/run_coverage_v2.py --season "${SEASON}"
          for f in data/cb_coverage_team.csv data/cb_coverage_player.csv data/wr_cb_exposure.csv; do test -s "$f"; done
          python - <<'EOF'
          import pandas as pd
          team = pd.read_csv("data/cb_coverage_team.csv")
          player = pd.read_csv("data/cb_coverage_player.csv")
          exposure = pd.read_csv("data/wr_cb_exposure.csv")
          if len(team) < 2 or len(team) % 2: raise SystemExit(f"FATAL: invalid scheduled coverage team rows={len(team)}")
          if player.empty or exposure.empty: raise SystemExit("FATAL: Coverage v2 lost the authoritative WR universe")
          if exposure['opponent'].fillna('').eq('').any(): raise SystemExit("FATAL: Coverage v2 has unresolved WR opponents")
          print(
              f"Coverage v2 OK: scheduled_teams={len(team)} WRs={len(exposure)} "
              f"direct_matchups={int(player['matchup_available'].sum())} "
              f"alignments={int(player['alignment_available'].sum())} "
              f"team_scheme_rows={int(team['coverage_available'].sum())}"
          )
          EOF

      - name: Build optional current-season PBP enrichment
        continue-on-error: true
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/build/pbp_features.py --season "${SEASON}" || true

      - name: Build PlayerForm v2 with historical priors
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/run_player_form_v2_loader.py --season "${SEASON}" --prior-season "${PRIOR_SEASON}" --date "${SLATE_DATE}"
          python scripts/enrich_player_scoring_v2.py --season "${SEASON}" --prior-season "${PRIOR_SEASON}" --date "${SLATE_DATE}"
          for f in data/player_game_logs.csv data/player_season_totals.csv data/player_form.csv data/player_form_consensus.csv; do test -s "$f"; done

      - name: Assemble deterministic metrics
        if: ${{ github.event.inputs.fetch_live_odds == 'true' }}
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/run_metrics_context.py --season "${SEASON}" --mode full --date "${SLATE_DATE}"
          python scripts/validate_build_integrity.py
          python scripts/metrics_ready.py --season "${SEASON}" --date "${SLATE_DATE}"

      - name: Price with joint Monte Carlo simulation
        if: ${{ github.event.inputs.fetch_live_odds == 'true' }}
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/run_pricing_v2.py --season "${SEASON}"
          python - <<'EOF'
          import pandas as pd
          from scripts.artifact_contracts import CONTRACTS
          c = CONTRACTS["props_priced"]
          df = pd.read_csv(c.path)
          assert not df.empty
          missing = [x for x in c.required_columns if x not in df.columns]
          assert not missing, missing
          assert "simulation_iterations" in df.columns
          print(f"priced rows={len(df)} simulations={df['simulation_iterations'].iloc[0]}")
          EOF

      - name: Strict repository audit
        run: |
          export PYTHONPATH="${PWD}"
          python scripts/utils/audit_repo.py --strict

      - name: Summarize outputs
        if: always()
        run: |
          find data outputs -maxdepth 2 -name "*.csv" -type f -print0 2>/dev/null | xargs -0 -r -I{} sh -c 'echo "--- {}"; wc -l "{}"'

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: run_${{ github.run_id }}
          path: |
            data/**
            outputs/**
          if-no-files-found: warn
          retention-days: 7
