name: M96B Advance Research Handoff

on:
  workflow_dispatch:
  push:
    branches:
      - research-rb-m96b-modular-joint-synthesis
    paths:
      - 'scripts/backtest/advance_m96b_handoff.py'
      - '.github/workflows/advance-m96b-research-handoff.yml'

permissions:
  contents: write

jobs:
  advance-handoff:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update canonical research handoff
        run: |
          if grep -q '^# NEXT MIGRATION — M96B' docs/migrations/CURRENT_NFL_RESEARCH_HANDOFF.md; then
            python scripts/backtest/advance_m96b_handoff.py
          elif grep -q '^# Latest completed migration: M96B' docs/migrations/CURRENT_NFL_RESEARCH_HANDOFF.md && grep -q '^# NEXT MIGRATION — M96C' docs/migrations/CURRENT_NFL_RESEARCH_HANDOFF.md; then
            echo 'M96B handoff already advanced; idempotent success.'
          else
            echo 'Unexpected handoff state; refusing silent rewrite.' >&2
            exit 1
          fi

      - name: Commit handoff
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/migrations/CURRENT_NFL_RESEARCH_HANDOFF.md
          if git diff --cached --quiet; then
            echo 'No handoff changes required.'
            exit 0
          fi
          git commit -m 'M96B: advance canonical research handoff to M96C'
          git push origin HEAD:research-rb-m96b-modular-joint-synthesis
