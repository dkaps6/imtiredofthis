# Football Context Qualification Execution Checkpoint V1

Status: ENGINEERING CHECKPOINT — NO PREDICTIVE RESULT

## Canonical branch head entering checkpoint

`00bd38e941eba6df8adc3b4f4359937af36fcd8e`

## What is now executable

The football-context program has a complete outcome-free qualification chain:

1. `build_context_stability_evidence.py` — strict-prior, season-bounded persistence evidence.
2. `build_context_candidate_profile.py` — coverage, stable-ID, unknown-state, duplicate-key, prior-support and stability attachment.
3. `build_context_signal_qualification_inventory.py` — fail-closed disposition into the frozen qualification classes.

The candidate-profile builder never reads target-game outcomes and never fits a projection model. Its canonical-key duplicate count, explicit unknown rate, eligible-row denominator and stable-ID coverage are calculated before any candidate can be considered experiment-ready.

## Repository-state finding

The branch does not commit the large generated historical player/team context tables. The tracked `data/` surface contains small configuration/identity/support assets, while canonical historical player/team rows are generated by the already-qualified historical builders and/or retained as finite-lifetime Actions artifacts.

Therefore the next evidence materialization must follow `HISTORICAL_DATA_REUSE_POLICY_V1.md`: reuse a preserved canonical artifact if available; otherwise deterministically rehydrate the exact historical base with the frozen builder. Absence of a row-level CSV from Git is not evidence that the historical source must be rediscovered.

## Qualification priority order

The first materialization pass should prioritize broad-coverage context before scarce tracking context:

1. role/environment regime signals;
2. personnel continuity / vacated opportunity;
3. team and player historical analog descriptors;
4. OL/DL continuity and protection context;
5. WR/TE defender-proximity exposure;
6. BDB geometry features with already-demonstrated persistence.

This order is not a predictive ranking. It minimizes wasted work by establishing broad historical coverage and redundancy first, then attaching more expensive/scarce geometry only where it has adequate support.

## Evidence required before a frozen predictive experiment

A candidate may not advance merely because it is football-plausible or persistent. It must have:

- pregame-known provenance;
- canonical grain and no join fanout;
- stable identity coverage meeting the frozen threshold;
- explicit missing/unknown semantics;
- sufficient historical/position coverage;
- sufficient strict-prior support;
- season-bounded stability evidence;
- a stated projection component/mechanism;
- a redundancy audit against already-known model inputs.

## Current blocker

No scientific blocker exists. The remaining blocker is materialization/CI execution: this connector session can inspect and commit ordinary repository files but cannot dispatch a workflow directly. A workflow-file write attempted in this checkpoint was blocked by the tool safety layer, so no CI result is claimed.

## Next authorized action

Materialize the first real role/personnel candidate tables from the canonical historical base, compute strict-prior stability and support profiles, then feed them into the qualification inventory. Only candidates that clear the frozen gates should receive a separately frozen predictive experiment plan.

## Scientific boundary

No production model change, no predictive fit, no target-game outcome lookup, no sportsbook teacher, no paid odds pull, no Issue #535 modification, and no reopening of failed-closed research families occurred in this checkpoint.
