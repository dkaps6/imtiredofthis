# Core scientific stack (Python 3.11+/3.12-friendly)
pandas>=2.1.4
numpy>=1.26
scipy>=1.11
statsmodels>=0.14.2

# IO / utils
requests>=2.31
openpyxl>=3.1
python-dateutil>=2.9
pytz>=2024.1
pyarrow>=14

# nflverse (python)
#
# The latest `nflreadpy` wheel on PyPI is 0.1.3 (Python ≤3.11).  To avoid the
# conflicting resolver requests you observed (simultaneous <0.3 and ≥0.3
# constraints), we pin the companion libraries to the last known compatible
# combination.  If newer releases appear, upgrade the pins together after
# verifying they coexist.
nfl_data_py>=0.3.3,<0.3.4
nflreadpy==0.1.3; python_version < '3.12'
polars>=0.20
beautifulsoup4>=4.12
lxml>=4.9.4,<5   # faster/more robust parser for bs4 (compatible with nflreadpy)

# IO / utils
requests>=2.31
openpyxl>=3.1
python-dateutil>=2.9
pytz>=2024.1
pyarrow>=14

# nflverse (python)
nfl_data_py>=0.3.3
# nflreadpy wheels are published through 0.1.3 today. Keep the dependency scoped
# to Python 3.11-and-earlier runners so installs succeed where wheels exist,
# while allowing future 0.2.x/0.3.x releases to satisfy the requirement.
nflreadpy>=0.1.3; python_version < '3.12'
polars>=0.20
beautifulsoup4>=4.12
lxml>=4.9.4,<5   # faster/more robust parser for bs4 (compatible with nflreadpy)

# IO / utils
requests>=2.31
openpyxl>=3.1
python-dateutil>=2.9
pytz>=2024.1
pyarrow>=14

# nflverse (python)
nfl_data_py>=0.3.3
# nflreadpy wheels are published through 0.1.3 today. Allow newer 0.2.x+ builds
# when they arrive, but keep the dependency scoped to Python 3.11-and-earlier
# runners so installs succeed where those wheels exist.
nflreadpy>=0.1.3,<0.3; python_version < '3.12'
polars>=0.20
beautifulsoup4>=4.12
lxml>=4.9.4,<5   # faster/more robust parser for bs4 (compatible with nflreadpy)
# Core scientific stack (Python 3.12-compatible wheels available on Actions)
pandas>=2.2.2
numpy>=1.26.4,<2.0
scipy>=1.11,<2.0
statsmodels>=0.14.2,<0.15

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.3,<0.4
# nflreadpy only publishes wheels up to 0.1.3 today. Keep that exact release on
# Python 3.11-and-earlier runners so we still hit the live nflverse mirrors,
# while Python 3.12 environments rely on the built-in nfl_data_py fallback.
nflreadpy==0.1.3; python_version < '3.12'
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9.4,<5   # faster/more robust parser for bs4 (compatible with nflreadpy)

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.3,<0.4
# nflreadpy only publishes wheels up to 0.1.3 today. Keep that exact release on
# Python 3.11-and-earlier runners so we still hit the live nflverse mirrors,
# while Python 3.12 environments rely on the built-in nfl_data_py fallback.
nflreadpy==0.1.3; python_version < '3.12'
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9,<6   # faster/more robust parser for bs4

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.3,<0.4
nflreadpy>=0.2
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9,<6   # faster/more robust parser for bs4
# Core scientific stack (Python 3.9+/GitHub Actions compatible)
pandas>=2.1.4,<2.2
numpy>=1.26.4,<2.0
scipy>=1.11,<2.0
statsmodels>=0.14.2,<0.15

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.3,<0.4
nflreadpy>=0.2
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9,<6   # faster/more robust parser for bs4
# Core scientific stack (Python 3.11/3.12 compatible)
pandas>=2.2.2,<2.3
numpy>=1.26.4,<2.0
scipy>=1.11,<2.0
statsmodels>=0.14.2,<0.15

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.4
nflreadpy>=0.2
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9,<6   # faster/more robust parser for bs4
pandas>=2.1.4,<2.3
numpy>=1.26.4,<2.0
scipy>=1.11,<2.0

# IO / utils
requests>=2.31,<3
openpyxl>=3.1,<4
python-dateutil>=2.9,<3
pytz>=2024.1,<2025
pyarrow>=14,<16

# nflverse (python)
nfl_data_py>=0.3.4
nflreadpy>=0.2
polars>=0.20.5
beautifulsoup4>=4.12,<5
lxml>=4.9,<6   # faster/more robust parser for bs4
pandas==2.1.4
# Core scientific stack (Python 3.12 compatible)
pandas==2.1.4
pandas==2.2.2
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.2
statsmodels==0.14.2
statsmodels>=0.14.1
# Core scientific stack (py3.11 compatible, matches nfl_data_py 0.3.x)
pandas==1.5.3
numpy==1.24.4
scipy==1.10.1
scikit-learn==1.3.2

# IO / utils
requests==2.32.3
openpyxl==3.1.5
python-dateutil==2.9.0.post0
pytz==2024.1
# PyArrow 15+ ships wheels for Python 3.12 (12.0.1 stopped at 3.11)
pyarrow==15.0.2

# nflverse (python)
nfl_data_py>=0.3.4
# (Optional) If you later want the newer python port, add:
nflreadpy>=0.2
polars>=0.20.5
pandas-datareader==0.10.0
beautifulsoup4==4.12.3
lxml==5.3.0   # faster/more robust parser for bs4
