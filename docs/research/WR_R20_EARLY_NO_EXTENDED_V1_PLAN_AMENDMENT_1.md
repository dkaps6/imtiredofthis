# WR-R20 Early / No-Extended Progression V1 — Plan Amendment 1

**STATUS: FROZEN PRE-RESULT CLARIFICATION ONLY. NO WR-R20 OUTCOME HAS BEEN SCORED. 2024 HOLDOUT SEALED.**

Canonical frozen plan:
- `docs/research/WR_R20_EARLY_NO_EXTENDED_V1_PLAN.md`
- plan commit `0f83ec22f22cdb7e35fcab7ef056225a7c806f54`

## Required Claude clarification — `CHK`

`CHK` remains excluded from the `EARLY_NO_EXTENDED_SHARE8` binary denominator because a checkdown is a **downstream progression outcome after earlier reads fail**; it is neither receiver-design/primary usage nor a clean numbered extended-read state. Including `CHK` in `EARLY_NO_EXTENDED` would contaminate the intended designed/primary mechanism with failed-progression fallback behavior.

`SD` remains excluded under the already-frozen plan because scramble-drill targets arise after the designed progression structure has broken down.

## No other change

This amendment changes **nothing else**. The following remain exactly frozen as written in the canonical plan:
- primary signal construction;
- 2022 / 2023+ semantic mapping;
- negative expected direction;
- last-8-before-filtering rule;
- 4-game / 16-classifiable-target support floor;
- team-scheme and target-depth controls;
- raw Stage-A gates and negative tail orientation;
- mediation equation and thresholds;
- identity/source/temporal rules;
- 2024 sealing;
- stop rules;
- no sportsbook / no production change.

Implementation and any later result must cite both the canonical frozen plan and this amendment.