# Phase 0 run checkpoint — normalized artifacts and source-window QA

**Branch:** `data-frontier-phase0-bdb-contact-v1`

**Starting head verified:** `7301e8322f055563be0683644c2f7b4a0e47f072`

## Scope

Data engineering / QA only. No predictive experiment, production science, Issue #535 direction, sportsbook logic, paid run, or frozen WR/RB/QB/TE plan was changed.

## Completed

1. Added `scripts/data_frontier/bdb_2024_artifact_qa.py`.
2. Added deterministic persistence of normalized `tracking`, `plays`, and `tackles` tables under an output `normalized/` directory so downstream artifacts remain auditable back to normalized source rows.
3. Added source-window diagnostics that count unique event frames and play-level presence for `ball_snap`, `handoff`, `tackle`, `out_of_bounds`, `touchdown`, `fumble`, and `qb_slide`.
4. Added frames-per-play min/median/max diagnostics.
5. Explicitly records the BDB 2024 event-window caveat: absence of an event from the published tracking window is not evidence the football event did not occur outside that window.
6. The wrapper reuses the frozen `BDB_2024_CONTACT_GEOMETRY_V1` detector and records `contact_detector_changed=false`.
7. Added `tests/test_bdb_2024_artifact_qa.py` covering event deduplication, missing-event behavior, and normalized-table persistence.

## Validation state

A local clone/test execution was attempted from the automation runtime, but that runtime could not resolve `github.com`, so the new tests were not falsely reported as executed. The tests are committed for the next environment with repository execution/network access. Prior frozen detector tests are unchanged by this checkpoint.

## Access boundary

Real BDB 2024 competition files remain unavailable in this environment without authenticated competition access/rule acceptance. No terms were accepted and no dataset was purchased or downloaded.

## Next checkpoint

1. Wire the deterministic geometry derivatives (closing-speed proxy, pursuit-angle error, sideline distance, source-label overlap breakdown) into a separate enriched artifact without changing the frozen detector.
2. Add explicit provenance/version fields for every normalized/derived artifact.
3. Execute the complete Phase-0 test set when an execution environment can access the branch.
4. Run the real BDB benchmark only when the competition files are lawfully available under already-accepted access.
